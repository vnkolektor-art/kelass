/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/NAMA-REPO',
  assetPrefix: '/NAMA-REPO/',
};

export default nextConfig;
